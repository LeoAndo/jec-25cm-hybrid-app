name: f02_stamp_release
description: "習慣を追加して保存し、APKで提出するスタンプ帳。"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.4

dependencies:
  shared_preferences: 2.5.5
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
