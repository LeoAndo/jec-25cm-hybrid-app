# 用語集：العربية（ar）

ハイブリッドアプリ開発技法の対訳表。訳すときは、この表の訳語を使う。新しく訳語を決めた用語は、行を足す。全言語に共通の翻訳ルールは `skills/translate-teaching-materials/SKILL.md` にある。メモに「要確認」とある行は、訳語に自信がないもの。翻訳PRで確かめたら「要確認」を消す。

**アラビア語は右から左に書く（2026-09-25 issue #39）。** 教科書のページは `<html dir="rtl">` で出る。ただし、訳文に日本語や英語のまま残す部分は左から右に並ぶので、その書き方に決まりがある（下の「書き方の決まり」）。訳文は現代標準アラビア語（فصحى）で書く。

## 製品名（訳さない）

| 日本語 | 訳 | メモ |
| --- | --- | --- |
| Monaca | Monaca | 通常版Monaca（Freeプラン）。Monaca Education とは別のサービス |
| Monaca クラウド IDE | Monaca クラウド IDE | 正式表記をそのまま保持する |
| Flutter | Flutter | |
| Dart | Dart | |
| Visual Studio Code | Visual Studio Code | 正式表記をそのまま保持する |
| Google Chrome | Google Chrome | |
| Xcode | Xcode | |
| Android Studio | Android Studio | Androidエミュレータと Android SDK のために使う |
| Cordova | Cordova | Apache Cordova |
| Onsen UI | Onsen UI | |

## 両科目に共通の用語（jec-25cm-kotlin とそろえる）

同じ学生が Kotlin演習（jec-25cm-kotlin）も受けている。jec-25cm-kotlin には、まだアラビア語の用語集がない（2026-09-25時点）。あちらにアラビア語を足すときは、この表の訳語にそろえる。そろえたあとで変えるときは、あちらも同じように直す。

| 日本語 | 訳 | メモ |
| --- | --- | --- |
| プロジェクト | مشروع | 複数形は مشاريع |
| パッケージ | حزمة | `f01_hello_flutter`・`package:flutter/material.dart` などのパッケージ名は訳さない |
| 関数 | دالة | `function`（JavaScript）・`void main()`（Dart）の表記は原文のまま |
| 変数 | متغير | `let` / `const`（JavaScript）・`var` / `final`（Dart）の表記は原文のまま |
| 実行 | تشغيل | 動詞は شغّل。Monacaのメニュー名 **実行** は日本語のまま残し (تشغيل) を添える。Visual Studio Code のボタン名は **Run** のまま |
| コンソール | وحدة التحكم | Chromeのデベロッパーツールの Console、Visual Studio Code の **Debug Console** は原文のまま。要確認 |
| エミュレータ | محاكي | Androidエミュレータ（محاكي Android）。シミュレータと同じ語になるので、区別が要る文では Android・iOS を必ず添える |

## この授業の用語

| 日本語 | 訳 | メモ |
| --- | --- | --- |
| ハイブリッドアプリ | تطبيق هجين | |
| ネイティブアプリ | تطبيق أصلي | |
| アプリ | تطبيق | |
| クロスプラットフォーム | متعدد المنصات | |
| WebView | WebView | Android の `WebView`、iOS の `WKWebView` に当たる。訳さない |
| クラウドIDE | بيئة التطوير السحابية | ブラウザで使う開発環境。Monacaの画面名として出るときは日本語のまま残す |
| テンプレート | قالب | |
| 公開 | نشر | Monacaの **プロジェクト → 公開…**。発行されるのはプロジェクトを取り込ませるURLで、アプリを動かすURLではない。前年度の「Web公開」（استضافة الويب）とは別物なので、استضافة とは訳さない |
| 公開URL・取り込み用のURL | رابط الاستيراد | `https://monaca.mobi/ja/directimport?pid=…` の形のURL。開くと、そのプロジェクトが自分のMonacaに取り込まれる |
| インポート・取り込む | استيراد | ダッシュボードの **インポート** は日本語のまま残す。Dart の `import` 文は `<code>` の中なので関係ない |
| エクスポート | تصدير | Freeプランでは使えない |
| プレビュー | معاينة | クラウドIDEの画面名 **プレビュー** は日本語のまま残す |
| ビルド | بناء | 動作は «تنفيذ البناء» とも書く。build と英字のまま書く資料もある。要確認 |
| クラウドビルド | البناء السحابي | ビルドの訳語にそろえる |
| デバッグ | تصحيح الأخطاء | |
| 実機 | جهاز فعلي | |
| シミュレータ | محاكي | iOSシミュレータ（محاكي iOS）。エミュレータと同じ語になる（上の表） |
| デバイス | جهاز | Visual Studio Code のデバイス選択の画面名は英語のまま |
| ウィジェット | Widget | 英字のまま。定まった訳がない（ودجت と書く資料もある）。個々のクラス名（`Text`、`Scaffold`）は `<code>` の中なので訳さない。要確認 |
| 宣言的UI | واجهة المستخدم التصريحية | |
| 状態 | الحالة | `setState` は `<code>` のまま |
| ホットリロード | Hot Reload | 英字のまま。إعادة التحميل السريع と訳す資料もある。要確認 |
| 画面遷移 | التنقل بين الشاشات | `Navigator` は `<code>` のまま |
| 非同期 | غير متزامن | `async` / `await` は `<code>` のまま |
| コマ | الحصة | 1コマ＝90分の授業1回。「2コマ目」は الحصة 2 |

## Web基礎で習った用語

学生が履修済みの用語。定訳を使い、説明を足さない。

| 日本語 | 訳 | メモ |
| --- | --- | --- |
| 要素 | عنصر | HTMLの要素 |
| 属性 | سمة | HTMLの属性 |
| イベント | حدث | |

## Monacaの画面に出る言葉（日本語のまま残し、訳をかっこで添える）

Monacaは日本語表示で使う。画面の言葉は訳さずに残し、うしろにこの表の訳をかっこで添える（例：<code>プロジェクト → 公開…</code> (مشروع ← نشر…)）。かっこの中はアラビア語の並びなので、矢印は ← にする（下の「書き方の決まり」）。かっこの訳は意味を伝えるためのもので、Monacaのほかの言語の表示と同じとは限らない。

| 画面の言葉 | かっこに添える訳 | 場所 |
| --- | --- | --- |
| ファイル | ملف | クラウドIDEのメニュー |
| 編集 | تحرير | クラウドIDEのメニュー |
| 表示 | عرض | クラウドIDEのメニュー |
| 実行 | تشغيل | クラウドIDEのメニュー |
| ビルド | بناء | クラウドIDEのメニュー |
| プロジェクト | مشروع | クラウドIDEのメニュー |
| 設定 | إعدادات | クラウドIDEのメニュー |
| ヘルプ | مساعدة | クラウドIDEのメニュー |
| 公開… | نشر… | プロジェクトメニュー |
| インポート | استيراد | ダッシュボード |
| 新しいプロジェクトを作る | إنشاء مشروع جديد | ダッシュボード |
| クラウドIDEで開く | فتح في بيئة التطوير السحابية | ダッシュボード |
| 最小限のテンプレート | قالب الحد الأدنى | 新規作成のテンプレートの選択肢 |
| プレビュー | معاينة | クラウドIDEの画面 |

## 書き方の決まり

右から左の文の中での見え方は、Google Chrome で文字ごとの表示位置を測って確かめた（2026-09-25、issue #39）。

- **日本語や英語のまま残す言葉が記号（。、…！？）で終わり、うしろにアラビア語が続くときは、その記号のすぐうしろに `&lrm;` を書く。** 書かないと、その記号が言葉の反対側へ回り込み、「。保存した記録を読み込みました」「…File &gt; Open Folder」のように見える。`&lrm;` は「左から右」を示す見えない文字で、対訳カタログの検査を通る。

  ```text
  原文：「保存した記録を読み込みました。」、追加した習慣、回数、詳細の理由を確認します。
  訳文：تحقق من ظهور 「保存した記録を読み込みました。&lrm;」 (تم تحميل السجل المحفوظ.)، ومن العادة المضافة، …
  原文：File &gt; Open Folder…でF01StampBoardそのものを開く。
  訳文：افتح F01StampBoard نفسه من File &gt; Open Folder…&lrm; في Visual Studio Code، …
  ```

  次の場合は書かなくてよい。どれも正しく並ぶことを確かめてある。
  - `<code>`・`<kbd>` の中（<code>プロジェクト → 公開…</code>、<code>let count = 0;</code>）。CSSで左から右に並べている。
  - `<strong>` の中（<strong>好きなものノートを作ります。</strong>）。CSSで、中の最初の文字の向きに並べている。
  - 記号で終わらない言葉（「テキストを変更」）。
- **「」は、日本語のまま残す言葉を囲むときだけ使う。** 右から左の文の中でも、かっこは向きを反転して正しく囲む形で出る。
- **アラビア語で書く操作や処理の並びは、矢印を ←（U+2190）にする。** 右から左に読むので、次の段階は左にある。→ のままだと、逆の順番に見える。日本語や英語のまま残す部分の中の → は、原文のまま残す（その部分は左から右に並ぶ）。

  ```text
  原文：入力→追加→詳細→戻る→再読み込みの順で動かし、
  訳文：شغّل التطبيق بالترتيب: الإدخال ← الإضافة ← التفاصيل ← الرجوع ← إعادة التحميل، …
  原文：クラウド IDEの <code>プロジェクト → 公開…</code> を開き、
  訳文：افتح <code>プロジェクト → 公開…</code> (مشروع ← نشر…) في بيئة التطوير السحابية، …
  ```

- **`<strong>` の中は、英字や日本語で書き始めてアラビア語を続けない。** `<strong>` は中の最初の文字で向きが決まるので、英字で始めると左から右に並び、読む順が逆になる。アラビア語を先に書く（<strong>ملف Dart</strong>）か、英字・日本語だけを入れる。
- **数字は 0〜9（西洋数字）で書く。** STEP番号・コード・画面の表示と同じ数字にする。アラビア・インド数字（٠١٢٣）は使わない。
- **句読点はアラビア語のものを使う。** 読点は「،」、疑問符は「؟」、セミコロンは「؛」。文末は「.」。
- **引用符は «…»。** 画面やアプリに出る日本語を囲むときは「」のまま残す（上の決まり）。
- **手順と学生への呼びかけは、命令形の男性形を総称として使う**（افتح، اختر، انقر）。アラビア語のソフトウェアの手順書の慣例に合わせた。先生は المعلم と書く。`config/i18n.json` のUI文言と翻訳の注記（فاسأل المعلم）も、この書き方にそろえてある。要確認（学生本人に、違和感がないかを確かめる）。
- **母音記号（ハラカート）は付けない。** 読み違えやすい語だけ、シャッダ（ّ）を付けてよい（نزّل، شغّل）。
- **英字の製品名やコード名に、定冠詞 ال を直接付けない。** 名詞を添えて書く（إطار Flutter، حزمة shared_preferences）。
